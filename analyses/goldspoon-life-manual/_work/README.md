# Temporary analysis workspace

This directory holds range-specific, source-grounded working notes used to assemble the contracted final artifacts for `금수저생활백서`. It stays inside the assigned output boundary.
